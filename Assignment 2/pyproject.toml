[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lns-arithmetic"
version = "0.1.0"
dependencies = ["numpy"]

[tool.setuptools.packages.find]
include = ["lns*"]
